# UX / 学習コンテンツ ブラッシュアップ バックログ

> 公開前の使い勝手・学習体験の洗い出し(2026-05-30)。**本書は洗い出しのみ。修正は別途検討して着手する。**
> 手法: 4次元の並列コード/コンテンツ監査(コーチ解説・ドリル・理論コンテンツ・ページ使い勝手)+ 実機(Playwright)観察。
> オーナー指摘の3点を最優先テーマに据える。
> **重要(ルール1)**: ここで提案する「リッチな解説文」は GTO 精度を過大主張してはならない。`source`(✓ソルバー解 / △近似)に整合する表現に留める。

## オーナーの問題意識(最優先)
1. **アクションの解答が端的すぎる**(コーチの「なぜ」が無い)→ テーマ A
2. **学習コンテンツが充実していない**(理論16概念/用語48は薄い)→ テーマ B
3. **説明が端的すぎる**(ドリルの 💡 が1行)→ テーマ C

総評: 構造(ナビ・モバイル・アクセシビリティ・正直な source 表示)は良好。**弱点は「説明の薄さ」と「学習導線の発見性」**で、せっかくの理論/ドリル/分析の相互リンク資産が初回ユーザーに届いていない。

---

## ✅ 実装状況(2026-05-30 並列ワークフローで全実装)

全テーマを Foundation→A/B/C/D 並列ワークフローで実装。**lint0・型0・332テスト緑・実機レンダリング確認(理論25概念・キー操作・0 console err)**。

- **Foundation ✅**: `src/lib/coach/coachConcepts.ts`(handTier / CATEGORY_EXPLAIN 全14 / preflopPrinciple / postflopPrinciple / boardTexture / conceptIdForCategory)+ `src/components/common/TermChips.tsx`(用語ポップ + ConceptLink)。+18テスト。
- **A コーチ解説 ✅** A1–A10: mistakeMessage に概念ラベル+ハンド階層+原則、approximate は頻度ギャップ、postflop は実役で原則、MistakeCard 展開「背景説明」+EV内訳、CoachToast に source/カテゴリ、TermChips/関連理論リンク。
- **C ドリル説明 ✅** C1–C8: 2–3文化(ポジション頻度/ボードテクスチャ/stack依存)、TermChips+関連理論、「どちらも正解」、source信頼度語。
- **B 学習コンテンツ ✅** B1–B11: **概念 16→25(+9: ボードテクスチャ/レンジ優位機構/ベットサイジング/ポラライズ/チェックレイズ/ターン/マルチウェイ/SPR/エクイティ分布)**・盤面例・決定木化・ヒューリスティック・「よくあるミス」追記。用語 48→56。被覆テスト維持。
- **D 導線/初回 ✅** D1–D8+C5: 空状態「はじめ方」・オンボ 5→6枚+学習の流れ・学習パス・弱点全概念・空状態CTA・理論の用語チップ・キー操作モーダル・モバイルナビ整理・復習パネル展開・ドリル説明。

> ✅ **B の内容監修済**(2026-05-31・3視点レビューWF: GTO正確性/アプリ整合/ルール1)。修正適用: turn-play の盤面例破綻を是正・エクイティ分布の説明を実装(10分位ヒストグラム)に一致・value-bluff の33%を「目安」に hedge・**RFI頻度を概念とアプリ実装値(BTN37%等)で整合**・polarization を EV 駆動で再定義・overbet/SPR/range-advantage を精緻化。概念25/用語56維持・332テスト緑。

> ✅ **使い勝手確認 + UX細部 polish 済**(2026-05-31・細部レビューWF + Playwright 実機)。実機: オンボ6枚・学習の流れ・D1空状態・D6キー操作モーダル すべて 0 console err で動作確認。修正: **TermChips ポップオーバーを Portal 化**(overflow 親でクリップする問題を根治・body に fixed 配置+viewport クランプ・実機で mountedOnBody/inViewport 確認)/ タップ域 <44px を是正(TermChip/ConceptLink/MistakeCard展開/WeaknessCardリンク/TheoryPage閉じる・用語ボタン → min-h-9〜11)/ focus-visible 追加(キーボードa11y)/ CoachToast 4.5→5.5秒(新文量に対応)/ モバイル折返し(ドリル制御行・復習パネル決定行・LearnPageグリッド・オンボモーダル max-h+overflow・各ページ padding p-4 sm:)。332テスト緑。

---

## テーマ A — コーチのアクション解説(オーナー #1)【最優先】

現状の文面(`CoachAgent.ts` `mistakeMessage` 付近)は実質1文:
`「-1.8BB。AKs の フォールド より、推奨は レイズ 2.5BB 100% です。」`
= **何を(推奨)** は言うが **なぜ** が無い。14種ある `MistakeCategory` は集計には使うが**解説文に一切使われていない**。

| # | 問題 | 提案(honest) | 該当 | 重大度 |
|---|------|------|------|------|
| A1 | ミス文に「概念ラベル」が無い | `MistakeCategory`→{ラベル, 一言理由} のマップを作り「概念: BB防御タイト — 相手オープンから降りすぎ。BBはワイド防御が必須」を付与 | `CoachAgent.ts:206–214` / `types/stats.ts` | 高 |
| A2 | ハンドクラスの文脈が無い(AAもA2sも同じ端的さ) | プリフロップでハンド階層(プレミアム/ブロードウェイ/ホイールA/コネクター/ジャンク)を添える | `CoachAgent.ts` preflop分岐 | 高 |
| A3 | アクション別 EV 内訳が無い(showEv時) | `showEv && sols>1` のとき「推奨 +X.XBB vs あなた +Y.YBB」を1行(差>0.5BBのみ) | `CoachAgent.ts` postflop分岐 / `MistakeCard` | 中 |
| A4 | 近似モード(EV=0)で頻度ギャップを示さない | 「推奨頻度との差 78% vs 0% = タイト過ぎ」と**スケール感**を出す(EVが無い分これが有効) | `CoachAgent.ts:212` tail | 中 |
| A5 | postflop に役の強さ/レンジ・ブロッカー理由が無い | 「トップペア+ブロッカー。被ベットに対し チェック45%(降ろし)/ベット55%(バリュー)」 | `CoachAgent.ts` postflop | 中 |
| A6 | mixed/correct も端的(「正解です。推奨:…」) | mixed は手固有の理由(例 A7s=board coverage vs range flexibility)。`MomentLesson` の定型文をスポット別に | `CoachAgent.ts:186–192` / `MomentLesson.tsx` | 中 |
| A7 | 「次はこうする」prescriptive な一言が無い | 頻出カテゴリに「👉 次のBB防御はワイドを意識(50%+守る)」 | `CoachAgent.ts` | 中 |
| A8 | コーチが**ドリルの良質な解説資産を再利用していない** | `preflopDrill`/`postflopDrill` の PRINCIPLE/ハンド階層を `lib/coach/coachConcepts.ts` に**共有抽出**し、コーチ・ドリル両方で使う | `CoachAgent.ts` ↔ `drill/*` | 高(=A群の土台) |
| A9 | 表示層が文1行のみ。`MistakeCard` に背景説明の展開、`CoachToast` に source/カテゴリ副題が無い | `MistakeCard` に折りたたみ「背景説明」、`CoachToast` に source バッジ+カテゴリ副題(text-xs→sm) | `MistakeCard.tsx` / `CoachToast.tsx` | 高 |
| A10 | フィードバックから理論への deep-link が無い(WeaknessCard には既にある) | 各 feedback kind/category → 概念ID のマップで「関連理論を読む ▶」(`goTo('theory',{conceptId})` 既存パターン流用) | `MistakeCard`/`MomentLesson` | 中 |

> **A の本命**: A8(共有 `coachConcepts` lib)を土台に A1/A2/A6 を載せると、コーチ文が「何を+なぜ+概念」に一気に厚くなる。コストは中。

---

## テーマ B — 学習コンテンツの厚み(オーナー #2)

理論 **16概念**(プリフロップ6/postflop4/数学4/メンタル2)+ 用語 **48**。GTO学習アプリとしては薄い。概念本文は「打てます」と**宣言的**で、条件分岐(IF…THEN)・ボード例・頻度・スタック依存が無い。

| # | 不足している概念/深さ | 提案 | 該当 | 重大度 |
|---|------|------|------|------|
| B1 | ボードテクスチャ(ドライ/ウェット/ペア/連結)の概念が無い | 新概念+各 postflop 概念にボード例2–3(A♠2♦3♣=ドライ等) | `concepts.ts` postflop | 高 |
| B2 | SPR/スタック深度の概念が無い(現代GTOの基礎) | 「スタック深度とSPR」概念(100/50/25/10BB でレンジが変わる理由) | `concepts.ts` | 高 |
| B3 | ベットサイジング理論が断片的(なぜ2.5BB/なぜ均一サイズ) | 「ベットサイジングの原則」概念(ポットコントロール/保護/抽出) | `concepts.ts` | 高 |
| B4 | レンジ優位/ナッツ優位の**仕組み**が Cbet 言及止まり | 機構+盤面例(BTN vs BB on A♠K♦Q♠ = BTN ナッツ優位) | `concepts.ts` | 中 |
| B5 | ポラライズ/マージは用語集にあるが概念本文で使われない | 「ベットサイジングとポラライズ」概念で文脈化 | `glossary.ts`↔`concepts.ts` | 中 |
| B6 | 概念が宣言的で**決定木**が無い | postflop 概念3–4件を「①盤面分類→②レンジ優位確認→③頻度」の表/ツリーに | `concepts.ts` | 中 |
| B7 | 数学概念に**実務ヒューリスティック**が無い | ポットオッズに「エクイティ% > コール額/(ポット+コール+相手ベット)% なら +EV」/ミックスに「±10%で同等EV・乱数で散らす」 | `concepts.ts` 数学 | 中 |
| B8 | 概念↔ミスの**逆リンク**が無い | 各概念末尾に「よくあるミス: preflop_too_wide(例: UTGで98o)」 | `concepts.ts` | 中 |
| B9 | 用語が定義のみ・例/相互リンク弱い・本文未連携 | 主要用語(ナッツ優位等)に盤面例。relatedTerms をクリック可能に | `glossary.ts` | 中 |
| B10 | チェックレイズ/ターン戦略/マルチウェイ等の不足 | 概念追加(check-raise / turn play / multiway は簡易言及) | `concepts.ts` | 中 |
| B11 | エクイティの可視化が無い(式のみ) | 「エクイティ可視化」概念 or Ranges タブにボード別エクイティ・ヒートマップ(R21 基盤あり) | `concepts.ts` / RangesPage | 中 |

> 標準的な GTO カリキュラムは ~40概念。**+8〜12概念 + 既存概念の深掘り(各+1–2段落、盤面例、頻度)**で「学べる範囲」が大きく広がる。**ルール1**: 頻度・盤面例は近似である旨を保ち、ソルバー厳密と誤認させない。

---

## テーマ C — ドリル説明の端的さ(オーナー #3)

`explainPreflop/Postflop/PushFold` はいずれも1–2行。役/カテゴリの**ラベル**は出すが「なぜその頻度/アクションか」が無い。

| # | 問題 | 提案 | 該当 | 重大度 |
|---|------|------|------|------|
| C1 | プリフロップ説明が1行(「バリュー or ブロッカーの4bet候補。」) | 2–3文に。ポットオッズ/エクイティ/ポジション頻度差(BTN44% vs UTG13%)を1文 | `preflopDrill.ts` explain | 高 |
| C2 | push/fold 説明が「+EV/-EV」のみ。**スタック依存の理由が無い** | 「浅いほど広く押せる(エクイティ実現が縮む)」を stack/hand 文脈で | `pushFoldDrill.ts` explain | 高 |
| C3 | postflop 説明が役×汎用原則のみ。盤面/ポジション/頻度根拠が無い | ボードテクスチャ分類+IP/OOP+「ポット2/3が基準」のサイジング根拠 | `postflopDrill.ts` explain | 高 |
| C4 | ドリル説明↔理論/用語の**リンクが無い**(資産が孤立) | 説明内の用語(ブロッカー/レンジ優位)を用語ポップ、フッターに「関連理論を読む」deep-link | `drill/*` / `concepts/glossary` | 高 |
| C5 | 「ドリルとは/正解判定の意味(頻度10%)」の初回説明が無い | LearnPage ドリルタブに折りたたみのミニ説明 | `LearnPage.tsx` drill tab | 中 |
| C6 | サイズラベル「(ポットの67%)」の**読み方**が無い | 「GTOはポット比約2/3を基本、手で変えない(読まれるため)」注記 | `PostflopDrillPanel.tsx` | 中 |
| C7 | 推奨表示が文字のみ・複数正解が伝わりにくい | 推奨を頻度バー/全推奨を強調「(どちらも正解)」 | `PostflopDrillPanel`/`DrillQuestion` | 低 |
| C8 | 説明文に source 信頼度の語が無い(バッジのみ) | solver_precomputed=「厳密解」/ solver_live=「簡易求解・参考値」を1語添える | `PostflopDrillPanel` | 低 |

---

## テーマ D — 学習導線 / 初回体験(発見性)

| # | 問題 | 提案 | 該当 | 重大度 |
|---|------|------|------|------|
| D1 | **初回の空状態が素っ気ない**(実機確認: 「ハンドを開始してください」+ボタンのみ) | sessionHandCount===0 で「最初のハンド後にコーチ解説。用語は『理論』で」等のコールアウト | `GamePage.tsx` empty | 高 |
| D2 | オンボーディングが play へ橋渡ししない(play/study の違いが端的) | 6枚目「最初のハンド」or post-onboarding の対話的1ハンド。play/study を具体化 | `OnboardingFlow.tsx` | 高 |
| D3 | **理論↔ドリル↔分析のループが新規ユーザーに発見されない** | Dashboard に「学習パス」図(Game→ミス→分析→理論→ドリル→反復)。新規時は理論タブを先頭に | `LearnPage.tsx` | 高 |
| D4 | 空状態(Learn/Analysis)が静的「Gameでプレイすると…」 | 「まず基礎ドリル→ポジション理論→実践」の推奨パス+CTA | `LearnPage`/`AnalysisPage` | 中 |
| D5 | 用語がインラインでクリックできない(都度 用語集へ移動) | コーチ/理論/ドリル中の用語を ⓘ ポップで即定義 | `glossary` 横断 | 中 |
| D6 | キー操作(f/c/r/Space/?)がアプリ内に無い(CLAUDE.md のみ) | New Hand 付近に ⌨️「キー操作」モーダル | `GamePage.tsx` | 低 |
| D7 | モバイル下部6タブが等価・学習系が埋もれる | 学習系のグルーピング or サブメニュー、タップ域≥44px(維持確認)、戻る導線 | `AppShell.tsx` | 中 |
| D8 | play の `PostflopReviewPanel` 各行が1文・展開無し(今セッション追加分) | 各決定に 💡 展開(原則+理論リンク)/StrategyBars インライン | `PostflopReviewPanel.tsx` | 中 |

---

## 実機観察メモ(Playwright・2026-05-30)
- 起動・ナビ・理論ページ遷移とも **console エラー 0**(warning 1)。構造は健全。
- **ゲーム空状態**: 「ハンドを開始してください」+「New Hand (Space)」+前提条件フッターのみ = D1 を裏付け(初回の指針ゼロ)。
- **理論ページ**: 「戦略理論 / 用語集」タブ + フィルタチップ + 概念リスト。構造は良いが中身の薄さ(テーマB)は要対応。
- ※ HU postflop の実コーチ文面はランダムディール依存で特定再現が難しく、文面の薄さはコード監査(テーマA)で担保。

---

## 修正方針の検討(提案・未着手)

オーナー判断用に「波」で整理。**ルール1遵守**(近似を厳密と称しない)を全波で厳守。

- **Wave 1(オーナー指摘の直撃・中コスト・高効果)**: A8 共有 `coachConcepts` lib → A1/A2/A4/A6 でコーチ文を「何を+なぜ+概念」に / C1–C4 ドリル説明の深掘り+理論リンク。**= 既存の理論/PRINCIPLE 資産の再利用が中心で、新規コンテンツ生成は最小。**
- **Wave 2(導線・初回体験)**: D1/D2/D3/D4(空状態の指針・学習パス・オンボ橋渡し)+ A10/C4/D5(feedback/ドリル→理論・用語のリンク)。
- **Wave 3(コンテンツ拡充・要監修)**: B1–B11 の概念追加・深掘り。**GTO内容なので監修必須**(R11 と同じく近似表記・実解化は別軸)。最大の工数だが「学習アプリ」の看板に直結。
- **Wave 4(細部)**: A3/A5/A9/C6–C8/D6/D7/D8。

各 Wave は独立に着手可。Wave 1 は「薄い解説」のオーナー指摘に最短で効く。Wave 3 はコンテンツライティング+監修の比重が大きい。

> 次アクション: この洗い出しを基に、**どの Wave / 項目から着手するか**を選定 → 個別に実装。
